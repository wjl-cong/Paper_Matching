# 期刊论文语义匹配系统 — 系统设计文档

---

## 1. 系统概述

### 1.1 目标

实现一个能够**抓取指定期刊论文**、并通过**语义匹配**判断用户输入主题是否存在相似论文的系统。核心难点在于：
- 国内外多源异构数据的统一获取
- 中英文跨语言语义匹配
- 现场快速实现（面试场景）

### 1.2 输入输出

| 组件 | 描述 |
|------|------|
| **输入** | 10本期刊的 ISSN 列表（国内外各5本）+ 用户查询主题 |
| **输出** | 每本期刊是否存在相似论文（相似度分数 + 论文标题） |

### 1.3 数据规模

- 每个期刊获取中英文论文各约200篇
- 10个期刊约2000-4000篇论文
- 向量维度：1024维（BGE-M3模型）

---

## 2. 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                      表现层 (Presentation)                   │
│              CLI 命令行  │  FastAPI Swagger                   │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                      接口层 (API Layer)                      │
│        FastAPI 应用层  │  Pydantic 请求/响应模型              │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                    业务逻辑层 (Service Layer)                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │JournalService│  │SearchService │  │EmbeddingService  │  │
│  │期刊论文抓取 │  │  检索路由   │  │  向量化核心     │  │
│  └──────────────┘  └──────────────┘  └──────────────────┘  │
│  ┌──────────────┐  ┌──────────────┐                        │
│  │TranslationSvc│  │DatabaseService│                        │
│  │ 翻译服务(可)│  │  SQLite+FAISS │                        │
│  └──────────────┘  └──────────────┘                        │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                      数据层 (Data Layer)                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │  API Clients │  │  SQLite DB   │  │   FAISS Index    │  │
│  │ OpenAlex/   │  │  论文元数据  │  │   向量索引       │  │
│  │ CrossRef    │  │  本地缓存    │  │   语义搜索       │  │
│  └──────────────┘  └──────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                    模型层 (Model Layer)                      │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  BGE-M3 Embedding (多语言向量化，支持中英文跨语言)    │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 模块详细设计

### 3.1 数据获取模块 (JournalService)

#### 策略：多层降级获取

```
目标：获取期刊论文（中英文各约200篇）

┌─────────────────────────────────────────┐
│  Layer 1: OpenAlex API (优先)           │
│  - 按 ISSN 筛选 Source                   │
│  - 按 date 排序获取最新论文             │
│  - 支持语言筛选 (language:zh/en)        │
├─────────────────────────────────────────┤
│  Layer 2: CrossRef API (备选)           │
│  - 按 ISSN 检索 journal works            │
│  - 字段补全                            │
├─────────────────────────────────────────┤
│  Layer 3: 期刊名称搜索 (兜底)           │
│  - 使用期刊名称关键词搜索               │
└─────────────────────────────────────────┘
```

#### API限流处理

```python
# 限流配置
REQUEST_DELAY = 1      # 请求间隔（秒）
MAX_RETRIES = 3        # 最大重试次数
RETRY_DELAY = 5        # 重试间隔（秒）
```

#### 数据过滤规则

1. **年份过滤**：只保留近5年论文（2021年后）
2. **日期过滤**：排除未来日期（预发表论文）
3. **去重**：根据DOI去除重复论文

### 3.2 向量化模块 (EmbeddingService)

#### 向量构建策略

```
用户查询: "深度学习在医学影像中的应用"
     │
     ▼
┌─────────────────────────────────────────┐
│  BGE-M3 Embedding（推荐）                │
│  - 直接编码中/英文                       │
│  - 跨语言统一向量空间                   │
│  - 无需翻译即可匹配                     │
└─────────────────────────────────────────┘
```

**核心优势**：中文查询"深度学习"可以直接匹配英文论文"Deep learning takes on protein folding"，因为它们在向量空间中距离很近。

### 3.3 语义匹配模块 (SearchService)

#### 检索流程

```
用户输入: "深度学习在医学影像中的应用"
     │
     ▼
┌─────────────────────────────────────────┐
│  步骤1: 语言检测                        │
│  - 自动检测是中文还是英文                │
└─────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│  步骤2: 向量召回 (Vector Recall)        │
│  - 用户主题 Query → BGE-M3 向量化       │
│  - 用 FAISS Index 检索 Top-50           │
│  - BGE-M3 统一向量空间支持跨语言        │
└─────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│  步骤3: 过滤                            │
│  - 年份过滤：只保留近5年论文            │
│  - 阈值过滤：相似度 >= 0.1             │
│  - 未来日期过滤：排除预发表论文         │
└─────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│  步骤4: 按期刊分组                       │
│  - 按 ISSN 分组                         │
│  - 每期刊取 Top-5                       │
└─────────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│  步骤5: 返回结果                        │
│  - 相似度分数 + 论文标题 + 发表日期      │
└─────────────────────────────────────────┘
```

#### 为什么不用关键词过滤？

**跨语言场景下关键词过滤会误杀正确结果**：

例如查询 "深度学习在医学影像中的应用"：
- 提取关键词：`["深度学习", "医学", "影像"]`
- 英文论文 "Deep learning takes on protein folding" 不包含这些关键词
- 如果启用关键词过滤，这篇正确匹配的论文会被过滤掉

**解决方案**：依赖 BGE-M3 的向量语义匹配，确保检索结果的语义相关性。

#### 年份过滤函数

```python
def is_paper_recent(paper: Paper) -> bool:
    """判断论文是否是近期论文（近5年）"""
    if not paper.published_date:
        return True  # 没有日期的论文保留
    return paper.published_date.year >= MIN_PAPER_YEAR
```

### 3.4 翻译服务 (TranslationService)

翻译服务是**可选的增强功能**，BGE-M3 本身已经支持跨语言检索。

```python
class TranslationService:
    """翻译服务：用于跨语言检索增强（可选）"""

    def detect_language(self, text: str) -> str:
        """检测语言：返回 'zh' 或 'en'"""

    def zh_to_en(self, text: str) -> str:
        """中文翻译为英文（可选）"""

    def en_to_zh(self, text: str) -> str:
        """英文翻译为中文（可选）"""
```

---

## 4. 核心难点解决方案

### 4.1 国外期刊访问困难

| 难点 | 解决思路 |
|------|----------|
| 期刊网站被墙 | **不爬网站**，直接用 OpenAlex/CrossRef 等开放 API |
| API 在中国不稳定 | 添加重试机制 + 多 API 源降级 + 本地缓存 |
| API 限流 (429错误) | 请求间隔控制 + 重试机制 |
| 中文期刊国际数据库支持差 | 期刊名称搜索 + CrossRef 备选 |

### 4.2 跨语言语义匹配

| 难点 | 解决思路 |
|------|----------|
| 中文查询匹配英文论文 | BGE-M3 统一向量空间，无需翻译 |
| 专业术语中英表达差异 | BGE-M3 预训练数据包含大量学术语料 |
| 关键词过滤误杀正确结果 | **关闭关键词过滤**，依赖向量语义匹配 |

### 4.3 论文数据不完整

| 问题 | 解决思路 |
|------|----------|
| 无摘要 | 用标题 + 关键词构建语义向量 |
| 中文期刊数据少 | 优先使用期刊名称搜索 |
| 预发表论文日期不准 | 过滤未来日期 |

### 4.4 面试现场快速实现

| 策略 | 说明 |
|------|------|
| 模块化架构 | 各模块独立，可单独测试和替换 |
| 降级策略 | 每一层都有备选方案 |
| 缓存优先 | 已抓取的论文缓存在 SQLite，避免重复请求 |
| 轻量级依赖 | 核心仅需 requests + faiss + sentence-transformers |

---

## 5. API 设计

### 5.1 命令行接口 (CLI)

```bash
# 初始化系统（抓取论文+向量化）
python main.py init

# 执行检索
python main.py search "深度学习在医学影像中的应用"

# 查看状态
python main.py status

# 启动API服务
python main.py serve
```

### 5.2 REST API

```
POST /api/search
  Body: { "query": "用户输入的主题", "top_k": 5, "threshold": 0.1 }
  Response: { results: { journal_name: { found, similar_papers } } }
```

---

## 6. 数据库设计

### SQLite 表结构

```sql
-- 期刊表
CREATE TABLE journals (
    issn TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    country TEXT,  -- 'CN' or 'INT'
    description TEXT,
    last_fetched DATETIME
);

-- 论文表
CREATE TABLE papers (
    doi TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    abstract TEXT,
    authors TEXT,  -- JSON array
    journal_issn TEXT,
    journal_name TEXT,
    published_date DATE,
    url TEXT,
    keywords TEXT,  -- JSON array
    language TEXT,  -- 'en' or 'zh'
    indexed_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 7. 关键配置

```python
# config.py

# 向量化模型
EMBEDDING_MODEL = "BAAI/bge-m3"
EMBEDDING_DIM = 1024

# 检索参数
TOP_K_RECALL = 50        # 召回阶段返回数
TOP_N_RERANK = 5         # 重排后返回数
SIMILARITY_THRESHOLD = 0.1  # 相似度阈值

# API 配置
REQUEST_TIMEOUT = 10
MAX_RETRIES = 3
REQUEST_DELAY = 1  # 请求间隔（秒）

# 数据过滤
MIN_PAPER_YEAR = 2021  # 只保留近5年论文
```

---

## 8. 测试结果

### 测试1：中文查询

```bash
python main.py search "深度学习在医学影像中的应用"
```

**结果**：
- 检索期刊数: 10
- 论文总数: 620
- 命中期刊数: 3
- 检索耗时: 3633ms

**命中论文示例**：
| 期刊 | 论文 | 相似度 |
|------|------|--------|
| Science | Deep learning takes on protein folding | 0.641 |
| 中国科学: 数学 | Interpreting deep learning by establishing... | 0.555 |
| Nature | Multiplexed magnetic resonance imaging | 0.545 |

### 测试2：英文查询

```bash
python main.py search "deep learn"
```

**结果**：
- 命中期刊数: 4
- 最高相似度: 0.681 (Deep learning takes on protein folding)

---

## 9. 后续优化方向

| 优化点 | 说明 |
|--------|------|
| Cross-Encoder重排 | 使用BGE-reranker进行二阶段精排 |
| 混合检索 | 融合向量检索+BM25关键词检索 |
| 增量更新 | 定期增量更新期刊论文 |
| GPU加速 | 使用GPU加速向量计算 |

---

*文档版本：v3.0 | 作者：AI Assistant | 日期：2026-05-16*
